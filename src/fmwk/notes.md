# Course
## Input sheet
Using the model to do analysis. Input sheet is important for that. Want to have multiple sets of inputs (Base cases), which you can select between.

Want a 'comparison scenario' also, not clear why though.

Scenario is a variation on a base case, say on different loan term sheets. Important to be able to _see_ which ones modify the base case.

## Outputs
Need a way to calculate and compare outputs. Totals/Aggregates on results.

Key metrics: (Blended Equity) IRR, 